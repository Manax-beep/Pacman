#pragma once

#ifndef MENU_H
#define MENU_H

using namespace std;
using namespace sf;

// А это уже контент файла .h, где и находятся все предварительные объявления
bool menu(RenderWindow &window); // прототип функции
//void playSE(Sound tap);
//void playM();
//void setting(RenderWindow &window, Sound tap);
#endif // Конец header guard